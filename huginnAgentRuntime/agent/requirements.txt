# Huginn agent-runtime Python 의존성.
# claude-agent-sdk 는 Claude Code CLI(@anthropic-ai/claude-code)를 subprocess 로 호출한다.
# 재현성을 위해 compatible-release 로 핀(0.2.x). 런너는 표준 asyncio 만 쓴다(anyio 불필요).
claude-agent-sdk~=0.2.93

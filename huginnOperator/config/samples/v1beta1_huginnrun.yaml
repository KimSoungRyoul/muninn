apiVersion: muninn.io/v1beta1
kind: HuginnRun
metadata:
  labels:
    app.kubernetes.io/name: huginnoperator
    app.kubernetes.io/managed-by: kustomize
  name: huginnrun-sample
spec:
  # TODO(user): Add fields here
